<%= stylesheet_link_tag("/stylesheets/ajay.css") %>


<% @pictures.each_with_index do |p, index| %>
    <%= image_tag(p.photo_url, :id => ('modal-trigger' if index == 0), :class => 'drop-shadow curved curved-hz-1 curved_image', :id => "img_#{index}" ) %>
    <%= p.description %>, <%= p.likes %>, <%= p.comments %>, <%= p.social_network %>

    <%= image_tag p.user_photo_url, :class => 'profile' %>
    <%= p.user_name %>
    
<script>
$('img_<%= "img_#{index}" %>').hover {
  function() {
  },
  function() {
  }
};
</script>
<% end %>
